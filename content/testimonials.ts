import type { Testimonial } from "./types";

// Intentionally empty: none of the current 6 featured projects have a real,
// attributed client quote on file. Add entries here as real testimonials
// come in — the Testimonials stage (components/home/Testimonials) renders
// nothing rather than a placeholder when this list is empty, so the site
// never ships a fabricated quote.
export const testimonials: Testimonial[] = [];
